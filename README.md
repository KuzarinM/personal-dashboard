# 🖥️ System Control Dashboard

Минималистичный, но мощный дашборд для HomeLab в стиле Cyberpunk/Terminal.
Поддерживает мульти-профили, интеграцию с Telegram, Google Calendar, погодой и мониторингом сети.

## ✨ Возможности

*   **Мульти-дашборды:** Создавайте отдельные экраны для работы, дома или игр (`/work`, `/gaming`).
*   **Безопасность:** Конфигурация защищена паролем (HTTP Basic Auth). Пароли не передаются на клиент.
*   **Telegram Радар:** Отображение непрочитанных сообщений из личного аккаунта (Userbot).
*   **Календарь:** Интеграция с Google Calendar (через iCal) + ручные события.
*   **Таймеры:** Обратный отсчет до ближайших событий.
*   **Редактор:** Встроенный веб-редактор JSON-конфига с подсветкой синтаксиса.
*   **Виджеты:** Погода (5 дней), IP/Провайдер, Статус системы.
*   **Docker:** Полная упаковка в контейнер.

## 🚀 Быстрый старт (Docker)

Это рекомендуемый способ запуска.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-username/my-startpage.git
    cd my-startpage
    ```

2.  **Запустите через Docker Compose:**
    ```bash
    docker-compose up -d --build
    ```

3.  **Откройте в браузере:**
    *   Перейдите на `http://localhost:3000`
    *   **Логин:** `admin`
    *   **Пароль:** `123` (можно сменить в конфиге)

## ⚙️ Конфигурация

Все настройки храните в папке `data/`.
Главный файл — `default.json`. Вы можете создавать новые файлы (например, `work.json`), и они будут доступны по адресу `/work`.

### Пример `default.json`

```json
{
  "settings": {
    "title": "Home",
    "masterPassword": "123", 
    "telegramSession": "строка_сессии...",
    "calendars": [
      {
        "name": "Work",
        "url": "https://calendar.google.com/.../basic.ics",
        "icon": "💼"
      }
    ]
  },
  "categories": [
    {
      "title": "Services",
      "items": [
        {
          "name": "Proxmox",
          "url": "https://pve.local:8006",
          "icon": "https://..."
        }
      ]
    }
  ],
  "events": [
    {
      "name": "Deadline",
      "date": "2025-12-31T23:59:00",
      "icon": "🔥"
    }
  ]
}
```

### Настройка Telegram (Userbot)

Чтобы видеть свои сообщения, нужно сгенерировать сессию.

1.  Убедитесь, что у вас установлен **Node.js** локально.
2.  В корне проекта выполните:
    ```bash
    npm install
    node setup-tg.js
    ```
3.  Введите номер телефона и код подтверждения.
4.  Скрипт выдаст **длинную строку-ключ**.
5.  Скопируйте её и вставьте в `default.json` (или другой дашборд):
    ```json
    "settings": {
       "telegramSession": "ваша_длинная_строка_здесь"
    }
    ```
6.  Сохраните конфиг через веб-интерфейс или перезапустите контейнер.

### Настройка Google Calendar

1.  Зайдите в настройки Google Calendar -> Выберите календарь.
2.  Найдите раздел **"Секретный адрес в формате iCal"**.
3.  Скопируйте ссылку.
4.  Вставьте её в массив `calendars` в конфиге.

## 🛠️ Разработка (Localhost)

Если вы хотите дорабатывать код:

1.  Установите зависимости:
    ```bash
    npm install
    ```
2.  Запустите режим разработки (Frontend + Backend):
    ```bash
    npm run dev
    ```
3.  Сервер будет доступен на `http://localhost:5173`.

## 📂 Структура проекта

*   `src/views/DashboardView.vue` — Основная логика и верстка дашборда.
*   `src/components/` — Виджеты (Telegram, Weather, Timers).
*   `server.js` — Backend на Express (API, Auth, Proxy, Telegram Client).
*   `data/` — Здесь лежат ваши JSON-конфиги (монтируется как Volume в Docker).

## 🛡️ Безопасность

*   Пароли (`auth`, `masterPassword`) и сессии Telegram **автоматически вырезаются** сервером при отправке конфига на фронтенд.
*   Приватные дашборды защищены `HTTP Basic Auth`.
*   Запросы к внешним API (Google, Telegram) идут через сервер, скрывая ваш IP от сервисов (если сервер за VPN).

